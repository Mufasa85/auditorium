# TODO — Correction page "Créer un compte" responsive (320px–480px)

- [ ] Analyser et ajouter des styles CSS dédiés à `.register-form-card` et `.register-form` pour limiter la largeur (max 420px), centrer, et ajouter des marges/paddings mobiles.
- [ ] Ajuster `.form-row`/espacements verticaux (`.form-group`, `.pw-strength`, `.form-checkbox`) sur `<=768px` et `<=480px`.
- [ ] Forcer `width: 100%` sur `input/select/button` et corriger `.select-wrapper` + `.toggle-pw` sur mobile.
- [ ] Corriger la section conditions d’utilisation : wrapping sans débordement, texte “responsive”, case alignée en haut à gauche.
- [ ] Vérifier alignement des icônes (input/select) et supprimer tout débordement horizontal.
- [ ] Appliquer les modifications dans `style.css`.
- [ ] Tests manuels sur largeurs 320/360/414/480.

